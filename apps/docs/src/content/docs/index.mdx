---
title: MSW Panel
description: Framework-agnostic devtools for Mock Service Worker.
template: splash
hero:
  tagline: Inspect registered MSW handlers, toggle them, switch endpoints between scenarios, and group them with feature tags, all without changing your existing setup.
  actions:
    - text: Get started
      link: guides/getting-started/
      icon: right-arrow
    - text: View demo
      link: demo/react/
      icon: external
      variant: minimal
    - text: How it works
      link: concepts/overview/
      icon: right-arrow
      variant: minimal
---

import PackageManagerTabs from "../../components/PackageManagerTabs.astro";

Add two packages to your existing MSW setup:

<PackageManagerTabs packageName="msw-panel" dev />

```tsx
import { createMswPanelController } from "msw-panel";
import { MswPanel } from "msw-panel/react";

const controller = createMswPanelController({ runtime: worker });

function App() {
  return (
    <>
      <Routes />
      <MswPanel controller={controller} />
    </>
  );
}
```

A floating button appears in the corner. Click it to open the panel and toggle
any handler on or off. Then opt into more when you want it: [feature tags](/msw-panel/guides/feature-tags/)
to organize a large handler list, and [scenarios](/msw-panel/guides/scenarios/) to switch an
endpoint between `empty` / `error` / `loading` states. Both layer onto the setup above
without reworking it.

[Vue adapter (#4)](https://github.com/barrymichaeldoyle/msw-panel/issues/4)
and [Svelte adapter (#5)](https://github.com/barrymichaeldoyle/msw-panel/issues/5)
are the planned next first-party adapters. [Solid adapter (#6)](https://github.com/barrymichaeldoyle/msw-panel/issues/6)
and [Angular adapter (#7)](https://github.com/barrymichaeldoyle/msw-panel/issues/7)
are under consideration after that. Reactions and comments on those issues help
determine priority.
