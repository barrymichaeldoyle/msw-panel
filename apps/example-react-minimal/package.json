{
  "name": "@msw-panel/example-react-minimal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "dev:e2e": "vite --host 127.0.0.1 --port 4174",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "msw": "catalog:",
    "msw-panel": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}
